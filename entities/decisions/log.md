# Log: Decisions

Append-only ledger. Never edit or delete prior entries — correct forward, per §5.

- 2026-07-06 | source: `816663` cascade fallout | action: scaffolded the `Decisions` domain (`index.md`, `catalog.md`, this `log.md`) and created [[add-outlet-country-aliases]] (status: accepted) | reasoning: `outlet` and `country` lacked an `aliases` field needed for bare-name wikilinks to resolve, discovered when `[[Outlet/8Days]]` auto-created a stray empty note.
- 2026-07-06 | source: outlet articleCount double-counting caught during 10-article batch cascade | action: created [[fix-articlecount-double-counting]] (status: accepted) | reasoning: articleCount was being wrongly incremented as if a cascade-built running tally, when it was actually a pre-computed grand total already covering the whole raw corpus.
- 2026-07-06T22:09:56 (backfilled — see reasoning) | source: doubled-slug filename convention retrospective | entity: [[drop-doubled-slug-filenames]] | action: created (status: accepted) | reasoning: this entry is backfilled now — the decision note was created earlier in the session but never logged at the time.
- 2026-07-06T22:09:57 (backfilled — see reasoning) | source: log entry format gap found while auditing this domain | entity: [[log-entry-format-timestamp-and-wikilink]] | action: created (status: accepted) | reasoning: this entry is backfilled now — the decision note was created earlier in the session but never explicitly logged as "created" (only referenced via the format-change marker below).
- 2026-07-06T21:58:18 | format change, per [[log-entry-format-timestamp-and-wikilink]] | entity: n/a (log-format marker) | action: converted every backticked entity-filename mention above to a real [[wikilink]] (pointing at each file's current name); entries above this line predate full timestamps and one-entity-per-line — entries below must have both | reasoning: wikilinks now navigate correctly from any log entry to the entity it describes; see the decision note for why existing entries were not split or given fabricated times.
- 2026-07-08T10:52:59 | source: issue-radar backtest over full corpus (9,299 articles) | entity: [[add-issues-domain]] | action: created (status: accepted); scaffolded `entities/issues/` domain and added `scripts/issue_radar.py` + `scripts/issue_radar_procedure.md` | reasoning: blowups showed 2–17 weeks of structural lead time (recurrence, breadth, institutional attachment) computable from existing frontmatter; the vault had no home for live risk assessments with status, score, rationale, and catalysts.
- 2026-07-09T11:40:30 | source: user request to model MINDEF role-acronyms (CDF, PS, CNV...) as time-varying offices | entity: [[add-appointments-domain]] | action: created (status: accepted); scaffolded `entities/appointments/` domain (`index.md`, `catalog.md`, `log.md`) and populated the 10 core MINDEF SG appointment notes | reasoning: acronyms bound to a person's `aliases` cannot resolve a bare acronym and mis-resolve across a handover (PS(Defence) passed Chan Heng Kee -> Joseph Leong within the monitoring window); an appointment-keyed entity with dated holders resolves role-acronyms to the holder valid at the article's date.
- 2026-07-09T11:52:00 | source: impact review of `scripts/` after adding the Appointments domain | entity: [[add-appointments-domain]] | action: updated (no status change) — added non-cascade carve-outs to `scripts/entity_cascade_procedure.md` (no `## Coverage`/`mentionCount`, never `patch_coverage.py`) and `scripts/query_procedure.md` (holder queries via `## Holders`/`currentHolder`); confirmed no code changes needed across check_links/fix_links/patch_coverage/issue_radar | reasoning: the cascade procedure universally assumed Coverage+mentionCount population, which would corrupt an appointment note (or hit patch_coverage's ValueError) if an article wikilinked one; docs now carve out the non-cascade domain.
- 2026-07-22T16:00:00 | source: full 16,837-note article metadata audit against preserved raw exports and the live MSM database | entity: [[enforce-article-quality-gates]] | action: created (status: accepted) — authorized source-backed article metadata repair, schema validation, and mandatory ingest/nightly quality gates | reasoning: syntactically valid YAML still contained malformed native IDs and unsupported enum values, while generated artifacts accepted them silently.
- 2026-07-23T09:37:59+08:00 | source: user request to develop the issue radar against product tables | entity: [[run-issue-radar-on-product-tables]] | action: created (status: accepted) — authorized replacing the stale `wiki.db` radar input with read-only canonical MySQL product tables, using UAT by default and explicit production selection | reasoning: the product tables already hold every radar input and avoid maintaining a second, stale article/tag representation.
- 2026-07-23T16:21:08+08:00 | source: user request to populate the six missing radar inputs and classify tone/event type with language-model evidence | entity: [[add-llm-radar-input-enrichment]] | action: created (status: accepted) — added a non-mutating-by-default, two-pass enrichment runner with confidence gates and review artifacts | reasoning: the current loose inputs have empty tags/outlet/country fields and uniform default classifications, so they require provenance-preserving enrichment before reviewed product-table loading.
- 2026-07-23T17:30:22+08:00 | source: user request to fix the enriched issue-radar database path | entity: [[add-enriched-input-database-bridge]] | action: created (status: accepted) — authorized a deterministic enriched-Markdown-to-UAT staging bridge, external-ID allocation, strict review queue, transactional validation, and exact issue-tag preservation in compiled article bodies | reasoning: the enrichment existed but had no safe route into the radar's shared article database.
- 2026-07-23T17:39:29+08:00 | source: user clarification that articles arrive through both DSTA raw deliveries and direct crawlers | entity: [[unify-dual-source-article-intake]] | action: created (status: accepted) — made `Inputs/articles/` the single shared processing doorway, added month-routing for loose crawler files, and required recursive enrichment/staging across loose and monthly inputs | reasoning: DSTA files require raw-to-input conversion while crawler items may already be in Inputs, but both must receive identical downstream gates before entering `entities/article/` and the radar database.
- 2026-07-23T18:02:01+08:00 | source: pipeline-efficiency review of the nine documented processing steps against the repo | entity: [[derive-database-from-compiled-wiki]] | action: created (status: accepted) — authorized making the compiled wiki the sole upstream of the article database, retiring `stage_mysql_feeds.py` and `stage_enriched_radar_inputs.py` in favour of one projection runner, and collapsing `scripts/sql/` to load → validate → commit-or-rollback | reasoning: the same source material was parsed twice into two representations that had to be reconciled by hand, while `index/wiki.db` already showed the correct derive-from-Markdown shape.
- 2026-07-23T18:02:01+08:00 | source: pipeline-efficiency review of the nine documented processing steps against the repo | entity: [[fold-enrichment-into-compile]] | action: created (status: accepted) — authorized folding enrichment into the compile runner, resolving outlet metadata from a generated `entities/outlet/` index, and replacing the two-pass agreement check with a single-pass confidence gate that holds failing articles in `Inputs/` | reasoning: `enrich_radar_inputs.py` was calling the model twice per article for outlet name and country that the 530 existing outlet notes already answer deterministically, and an article was taking two separate per-article passes where one suffices.
- 2026-07-23T18:02:01+08:00 | source: supersession review while accepting [[fold-enrichment-into-compile]] | entity: [[add-llm-radar-input-enrichment]] | action: updated — status accepted → superseded; added a "Superseded by" section naming the replacing note and carrying its evidence/confidence requirements forward | reasoning: its two-pass enrichment mechanism is replaced wholesale, and leaving it accepted would have left two contradicting decisions authorizing the same script.
